channels:
    - conda-forge
    - bioconda
dependencies:
    - cython
    - qurro
    - pip
    - numpy >= 1.12.0
    - zipp = 3.15
    - pandas>=1.0.0  # Relaxed constraint
    - songbird
    - iow

